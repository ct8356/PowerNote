<TreeView x:Class="PowerNote.EntriesTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PowerNote"
             xmlns:VMs="clr-namespace:PowerNote.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             ItemsSource = "{Binding EntryVMs}"
             Background="{StaticResource BackgroundBrush}"
             BorderBrush="{StaticResource BackgroundBrush}">
    <!-- I tried Binding to FilteredStudents (the queryable) but did not work. Unsurprising really. 
    Usually cannot bind to a queryable.-->
    <!-- were so close all along. Just forgot to make view students PUBLIC PROPERTY!-->
    <!--<TreeView.ItemTemplate>
        <HierarchicalDataTemplate ItemsSource="{Binding Children}">
            <local:EntryPanel/>
        </HierarchicalDataTemplate>
    </TreeView.ItemTemplate>-->
    <TreeView.Resources>
        <HierarchicalDataTemplate DataType="{x:Type VMs:StudentVM}"  ItemsSource="{Binding Children}">
            <local:EntryPanel/>
        </HierarchicalDataTemplate>
        <HierarchicalDataTemplate DataType="{x:Type VMs:PartVM}"  ItemsSource="{Binding Children}">
            <local:PartPanel/>
            <!--IT is recognising that it needs partPanel, I think-->
        </HierarchicalDataTemplate>
    </TreeView.Resources>
</TreeView>
